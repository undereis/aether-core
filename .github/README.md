# GitHub Automation

This directory is reserved for repository automation.

Phase 0 intentionally avoids enabling CI/CD before the local engineering
contract is validated. Future GitHub Actions workflows should run the same
commands exposed by the root Makefile.

