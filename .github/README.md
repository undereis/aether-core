# GitHub Automation

This directory contains repository automation and maintenance configuration.

The active CI workflow mirrors the local engineering contract exposed by the
root Makefile. It validates Rust formatting, Clippy, tests and builds, together
with Python locking, linting, formatting, typing, tests and package builds.
