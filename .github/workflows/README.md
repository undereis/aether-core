# Workflows

The active `ci.yml` workflow is a small wrapper around the repository-native
validation commands:

- `make lint`
- `make type-check`
- `make test`
- `make build`
- `make docker-validate`
