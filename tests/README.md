# Cross-System Tests

Repository-level integration and acceptance tests will live here when Aether
has multiple executable modules.

Phase 0 backend tests live under `backend/tests` because the backend owns those
contracts.

