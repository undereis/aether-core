# Backend

The backend is a Python 3.12 FastAPI application. In Phase 0 it exposes only
platform-level endpoints and infrastructure primitives:

- `/health`
- `/version`
- configuration loading
- structured logging
- dependency injection through FastAPI and an application container

No business capability belongs here yet.

